apiVersion: v1
kind: Service
metadata:
  name: ml-pipeline-ui
  namespace: kubeflow
spec:
  type: NodePort
  selector:
    app: ml-pipeline-ui
  ports:
  - name: http
    port: 80
    targetPort: 3000
    nodePort: 30502
